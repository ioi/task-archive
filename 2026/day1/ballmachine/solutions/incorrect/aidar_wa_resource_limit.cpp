#include "bits/stdc++.h"

#include "ballmachine.h"

using namespace std;
const int MAXN = 1000;

vector< int > find_structure(int m) {
    for (int i = 0; i < MAXN + 1; i++) {
        collect(); 
    }

    return {}; 
}

